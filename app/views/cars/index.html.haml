%h1.text-center.my-5.text-4xl Paganel Cars
- if search_params
  = render "params_table"
- if @cars.any?
  = render "sort_form"
  = render @cars
  .my-5.mx-auto.w-full.max-w-sm.md:max-w-xl.lg:max-w-2xl
    .digg_pagination
      .page_info
        = page_entries_info @cars
      %div
        = will_paginate @cars, :container => false, :page_links => false, class: "pages"
- elsif search_params
  %h3.text-center.my-5.text-2xl No car found according to your parameters.
- else
  %h3.text-center.my-5.text-2xl No cars have been added yet.
